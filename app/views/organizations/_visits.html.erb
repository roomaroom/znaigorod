<ul class='avatars'>
  <% @visits.each do |visit| %>
    <%= render partial: 'visits/visitor', locals: { visit: visit } %>
  <% end %>
</ul>

<!--
<div class="pagination">
  <%= link_to '&uarr;'.html_safe, '#', :class => "toggler #{ 'disabled' if @visits.first_page? }" %>
  <%= link_to '&darr;'.html_safe, organization_visits_path(@organization, :page => @visits.current_page + 1), :remote => true, :class => "next_page #{ 'disabled' if @visits.last_page? }" %>
</div>
-->
