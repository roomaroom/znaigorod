<%= render partial: 'filters_and_search' %>

<div class="organization_wrapper">
  <%= render partial: 'promo_block' %>

  <div class='organization_content js-button-pagination sauna_housing_content'>
    <ul class='posters js-paginable-list sauna_posters' data-item-width="220">
      <% if @presenter.special_case? %>
        <%= render :partial => "#{@presenter.category.slug}_tile_view_posters" %>
      <% else %>
        <%= render :partial => 'tile_view_posters' %>
      <% end %>
    </ul>
  </div>
</div>

