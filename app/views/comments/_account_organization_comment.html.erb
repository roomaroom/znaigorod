<% content_for :account_comment_link do %>
  <%= link_to comment.commentable.title, comment.commentable %>
<% end %>

<%= render :partial => 'account_comment_wrapper', :locals => { :comment => comment } %>
