<%= t("notice_mailer.comment_to_afisha") %>

<%= @comment.commentable.title %> (<%= polymorphic_url(@comment.commentable, :host => Settings['app']['host'] ) %>)

=========================

<%= @comment.user.name %> (<%= polymorphic_url(@comment.user.account, :host => Settings['app']['host'] ) %>)
<%= l(@comment.created_at, format: "%d %B %Y, %k:%M") %>
<%= @comment.body.truncated if @comment.body? %>

  <%= t("notice_mailer.answer") %>
  <%= afisha_path(@comment.commentable, :only_path => false, :anchor => "answer_#{@comment.id}") %>
