<%= t("notice_mailer.private_message") %>

=========================

<%= @message.producer.users.first.name %>
<%= l(@message.created_at, format: "%d %B %Y, %k:%M") %>

<%= @message.body.truncated if @message.body? %>

<%= t("notice_mailer.to_dialog") %> (<%= my_dialogs_path(:only_path => false, :utm_campaign => "znaigorod", :utm_medium => "email", :utm_source => "email") %>)
