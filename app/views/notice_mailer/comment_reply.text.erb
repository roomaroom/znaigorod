<%= @comment.user.name %>  (<%= polymorphic_url(@comment.user.account, :host => Settings['app']['host'], :utm_campaign => "znaigorod", :utm_medium => "email", :utm_source => "email" ) %>) <%= t("notice_mailer.comment_reply") %>

=========================

<%= @comment.commentable.title %>

<%= @comment.parent.user.name %>
<%= l(@comment.parent.created_at, format: "%d %B %Y, %k:%M") %>
<%= @comment.parent.body.truncated if @comment.parent.body? %>

  <%= @comment.user.name %>
  <%= l(@comment.created_at, format: "%d %B %Y, %k:%M") %>
  <%= @comment.body.truncated if @comment.body? %>

  <% if @comment.commentable.is_a?(Work) %>
    <%= work_path([@comment.commentable.contest, @comment.commentable], :only_path => false, :anchor => "answer_#{@comment.id}", :utm_campaign => "znaigorod", :utm_medium => "email", :utm_source => "email") %>
  <% end %>
  <% %w[afisha discount organization post].each do |item| %>
    <% if @comment.commentable.is_a?(item.camelize.constantize) %>
      <%= send("#{item}_path", @comment.commentable, :only_path => false, :anchor => "answer_#{@comment.id}", :utm_campaign => "znaigorod", :utm_medium => "email", :utm_source => "email")  %>
    <% end %>
  <% end %>
