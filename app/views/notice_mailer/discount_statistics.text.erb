<%= t("notice_mailer.discount_statistics") %>

=========================

<% @discounts.each do |discount| %>
  <%= discount.title %> (<%= discount_path(discount, :only_path => false, :utm_campaign => "znaigorod", :utm_medium => "email", :utm_source => "email" ) %>)
  Участники <%= discount.members.size %>
  Комментарии <%= discount.comments.size %>
  Голоса <%= discount.votes.liked.size %>
  Просмотры <%= discount.page_visits.size %>

=========================
<% end %>
