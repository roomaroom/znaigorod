<%= t("notice_mailer.comment_to_discount") %>

<%= @comment.commentable.title %> (<%= discount_path(@comment.commentable, :only_path => false, :utm_campaign => "znaigorod", :utm_medium => "email", :utm_source => "email" ) %>)

=========================

<%= @comment.user.name %> (<%= polymorphic_url(@comment.user.account, :host => Settings['app']['host'], :utm_campaign => "znaigorod", :utm_medium => "email", :utm_source => "email" ) %>)
<%= l(@comment.created_at, format: "%d %B %Y, %k:%M") %>
<%= @comment.body.truncated if @comment.body? %>

  <%= t("notice_mailer.answer") %>
  <%= discount_path(@comment.commentable, :only_path => false, :anchor => "answer_#{@comment.id}", :utm_campaign => "znaigorod", :utm_medium => "email", :utm_source => "email") %>
