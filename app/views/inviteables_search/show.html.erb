<ul>
  <% @results.each do |result| %>
    <li>
      <%= render :partial => "#{result.class.name.underscore}", :locals => { "#{result.class.name.underscore}".to_sym => result } %>
    </li>
  <% end %>
</ul>
