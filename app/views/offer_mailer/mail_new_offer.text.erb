На сайте ЗнайГород (<%= Settings['app']['host'] %>)

пользователь <%= @offer.name %> (<%= @offer.phone %>) добавил новое предложение цены.

<%= @offer.details %>

Готов заплатить: <%= number_to_currency(@offer.amount, :precision => 0) %>

Помотреть это предложение в manage: <%= polymorphic_url([:manage, :statistics, :offers], :host => Settings['app']['host'], :anchor => "offer_#{@offer.id}") %>
