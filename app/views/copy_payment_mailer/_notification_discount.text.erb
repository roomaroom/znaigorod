Завершилась продажа <%= "#{@paymentable.title} (#{discount_url(@paymentable)})" %>
Продано <%= "#{I18n.t('bet.copies', :count => @paymentable.copies.sold.count)}" %> по цене <%= number_to_currency @paymentable.price %>

Коды:
<% @paymentable.copies.sold.each do |copy| %>
  * <%= copy.to_s %>
<% end %>
