<% content_for :extra_meta do %>
  <% if page_meta %>
    <%= render :partial => 'commons/page_meta_og' %>
  <% end %>
<% end %>
<div class="presentation_filters posts">
  <div class="filter_header">
    <% if page_meta %>
      <h1><%= page_meta.header.squish.html_safe %></h1>
    <% else %>
      <h1><%= t('meta.posts.page_header') %></h1>
    <% end %>
  </div>

  <div class="order_by">
    <p>Сортировать по:</p>
    <ul>
      <% @presenter.order_by_filter.links.each do |link| %>
        <li class="<%= link.klass %>">
        <%= link_to link.title, link.path %>
        </li>
      <% end %>
    </ul>
  </div>
</div>

<div class="posts_list">
  <ul class="posters">
    <%= render :partial => 'posts/post_posters', :locals => { :collection => @presenter.collection } %>
  </ul>
</div>

<%= paginate @presenter.collection %>

<% if page_meta %>
  <div class='introduction'>
    <%= page_meta.html_introduction %>
  </div>
<% end %>
