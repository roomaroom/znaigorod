<div class="filters_wrapper">
  <div class="filters discount">
    <div class="by_type">
      <ul>
        <% @presenter.kind_filter.links.each do |link| %>
          <li class="<%= link.klass %>">
            <%= link_to link.title, link.path %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>

  <div class="filters discount">
    <div class="by_kind">
      <ul>
        <% @presenter.category_filter.links.take(8).each do |link| %>
          <li class="<%= link.klass %>">
            <%= link_to link.path do %>
              <span class="name"><%= link.title %></span>
              <span class="count"><%= link.results_count %></span>
            <% end %>
          </li>
        <% end %>

        <% if @presenter.category_filter.links.count > 8 %>
          <li class="more_link <%= 'selected' if @presenter.category_filter.more? %>">
            <a href='#'>&darr; Ещё обзоры</a>
          </li>
        <% end %>
      </ul>
    </div>
  </div>

  <% if @presenter.category_filter.links.count > 8 %>
    <ul class="more">
      <% @presenter.category_filter.links[8..@presenter.category_filter.links.count].each do |link| %>
        <li class="<%= link.klass %>">
          <%= link_to link.path do %>
            <span class="name"><%= link.title %></span>
            <span class="count"><%= link.results_count %></span>
          <% end %>
        </li>
      <% end %>
    </ul>
  <% end %>

  <div class="more_wrapper_toggler">
    <%= link_to 'Добавить обзор', new_my_post_path, :class => 'button' %>
  </div>
</div>

