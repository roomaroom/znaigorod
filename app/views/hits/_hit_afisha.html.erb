<%= content_tag :div, link_to(image_tag(resized_image_url(hit.result_decorator.poster_url, 88, 120), size: '88x120', alt: hit.result_decorator.title), afisha_show_path(hit.result_decorator)), class: :image %>

<div class="title">
  <%= link_to hit.title, hit.show_url %>
  <%= "(#{hit.result_decorator.model.original_title})" if hit.result_decorator.model.original_title.present? %>
</div>

<div class="info">
  <%= hit.result_decorator.when_with_price %>
  <%= hit.places %>

  <% if hit.result_decorator.has_tickets_for_sale? %>
    <div class='has_tickets'>
      <%= link_to 'Купить билет', afisha_show_path(hit.result_decorator, anchor: :buy_ticket), class: :button %>
    </div>
  <% end %>

  <div class='excerpt'><%= hit.excerpt %></div>

  <div class='details'>
    <%= content_tag :span, hit.result_decorator.visits.count, class: :to_go %>
    <%= content_tag :span, hit.result_decorator.comments.count, class: :comments %>
    <%= content_tag :span, hit.result_decorator.votes.liked.count, class: :likes %>
    <%= content_tag :span, hit.result_decorator.page_visits.count, class: :visits %>
  </div>
</div>

