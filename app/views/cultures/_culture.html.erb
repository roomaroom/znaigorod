<p>culture</p>
<% if culture.has_photogallery? %>
  <div class="photogallery">
    <ul>
      <% culture.images.each do |image| %>
        <li><%= link_to(image_tag_for(image.url, 124, 94), image.url, rel: :info_gallery) %></li>
      <% end %>
    </ul>
  </div>
<% end %>
