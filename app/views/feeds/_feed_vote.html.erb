<% if @feeds_presenter.is_discount?(feed_vote.feedable.class.name)  %>
  <%= render partial: "feeds/feed_vote_discount", :object => feed %>
<% else %>
  <%= render partial: "feeds/feed_vote_#{feed_vote.feedable.voteable.class.name.underscore}", :object => feed_vote unless feed_vote.feedable.voteable.class.name.underscore == "nil_class" %>
<% end %>
