<% content_for :breadcrumbs do %>

  <%#= render_navigation :renderer => :breadcrumbs, :join_with => content_tag(:span, "&nbsp;".html_safe, :class => "separator"), :context => :public %>

  <div id="breadcrumbs">
    <a href="#">Главная</a>
    <span class="separator">&nbsp;</span>
    <a href="#">Афиша</a>
    <span class="separator">&nbsp;</span>
    <a href="#">Кино</a>
  </div>

<% end %>

<div class="content_wrapper">

  <h1><%= @affiche.title %></h1>

  <%= @affiche.item_poster %>

  <%#= TODO fix error "undefined method `human_when' for nil:NilClass"  %>
  <%#= @affiche.when_with_price %>
  <%#/ TODO %>

  <% @affiche.places.each do |place| %>
    <%= place.place %>
  <% end %>

  <%= @affiche.html_description %>

  <% @affiche.tags.each do |tag| %>
    <%= link_to(tag, affiches_path(kind: @affiche.pluralized_kind, period: :all, tags: tag)) %>
  <%end%>
</div>
