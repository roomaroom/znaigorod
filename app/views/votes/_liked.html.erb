<% @users.each_slice(6) do |users| %>
  <ul class="liked">
    <% users.each do |user| %>
      <li>
        <% if user.profile %>
          <div class='user_item'>
            <%= link_to image_tag(user.avatar, :size => "80x80", :title => user.name), user.profile, target: '_blank' if user.avatar.present? %>
            <%= link_to user.name, user.profile, :class => 'name' %>
          </div>
        <% else %>
          <div class='user_item'>
            <%= image_tag user.avatar, :size => "24x24", :title => user.name %>
            <span class='name'><%= user.name %></span>
          </div>
        <% end %>
      </li>
    <% end %>
  </ul>
<% end %>

