<%= link_to t("invite.#{kind}"),
  polymorphic_path([:new, :invitation], 'invitation[kind]' => kind, 'invitation[invited_id]' => invited_id),
  :class => "#{kind}_button invitation_link",
  'data-title' => t("invite.#{kind}"),
  :remote => true %>
