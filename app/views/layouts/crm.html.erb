<!DOCTYPE html>
<html lang='ru'>
  <head>
    <meta charset='UTF-8'>
    <title>CRM</title>
    <%= favicon_link_tag %>
    <%= stylesheet_link_tag    'crm/application', :media => 'all' %>
    <%= javascript_include_tag 'crm/application' %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div class='header'>
      <%= link_to 'Система управления взаимоотношениями с клиентами', crm_root_path, class: :crm_logo %>
      <% unless @current_user.blank? %>
        <div class='user_box'>
          <%= @current_user.name %>
          <%= link_to 'Выход', manage_session_path(current_user), method: :delete, class: :sign_out %>
        </div>
        <div class="search">
          <% if resource_class == Organization %>
            <%= form_tag manage_organizations_path, :method => :get do %>
              <div class='wrapper'>
                <%= text_field_tag 'q', params[:q], :placeholder => 'Поиск' %>
                <%= submit_tag 'Поиск', class: :button %>
              </div>
            <% end %>
          <% end %>
          <% if resource_class == Affiche %>
            <%= form_tag manage_affiches_path, :method => :get do %>
              <div class='wrapper'>
                <%= text_field_tag 'q', params[:q], :placeholder => 'Поиск' %>
                <%= submit_tag 'Поиск', class: :button %>
              </div>
              <div class="include_gone">
                <%= check_box_tag 'include_gone', '1', params[:include_gone] %>
                <%= label_tag 'include_gone', 'Включая прошедшие' %>
              </div>
            <% end %>
          <% end %>
        </div>
        <%= render_navigation :context => 'manage' %>
      <% end %>
    </div>
    <div class='content'>
      <%= yield %>
    </div>
  </body>
</html>
