<!DOCTYPE HTML>
<html lang="ru">
  <head>
    <meta charset="UTF-8">
    <meta name='yandex-verification' content='6f2efa53b5d49370' />
    <title><%= (yield :subtitle).squish %><%= " — ".html_safe if content_for?(:subtitle) %><%= t('site_title') %></title>
    <%= favicon_link_tag %>
    <%= yield :stylesheets %>
    <%= yield :javascripts %>
    <%= yield :extra_meta %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <%= yield :content %>
  </body>
</html>
