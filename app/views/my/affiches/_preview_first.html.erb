<div class='title'></div>
<div class='description'>
  <div class='poster'>
    <% if @affiche.poster_image.exists? %>
      <%= image_tag @affiche.poster_url, size: '150x200', alt: :poster %>
    <% else %>
      <%= image_tag 'public/affiche_poster_stub.png', size: '150x200', alt: :poster %>
    <% end %>
  </div>
  <div class='text'>
  </div>
</div>
<div class='tags'></div>
