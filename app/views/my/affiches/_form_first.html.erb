<%= simple_form_for @affiche, url: my_affiche_path(@affiche) do |f| %>
  <%= hidden_field_tag :step, @step %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :tag %>
  <div class='submit'>
    <%= f.submit %>
  </div>
<% end %>
