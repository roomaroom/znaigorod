<%= link_to image_tag(review.annotation_image(width, height), :size => "#{width}x#{height}"), my_review_path(review.id),
  :class => 'js-video-preview',
  :data => { :video => { :uid => review.content_parser.videos.first.uid, :width => '700' } } %>
