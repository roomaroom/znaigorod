<div class="my_wrapper">
  <%= render_navigation context: 'my', :renderer => :breadcrumbs %>

  <div class="left">
    <div class="header_with_marker">
      <div class="to_right">
        <% if @review.draft?  %>
          <%= link_to t('my.send_to_public'), publish_my_review_path(@review), :method => :put, :class => 'button change_state',
            :confirm => 'Вы уверены? После публикации обзор будет доступен всем пользователям сайта!' %>

          <%= link_to poster_edit_my_review_path(@review), :class => 'button change_poster', :title => 'Изменить постер' do %>
            <span></span>
          <% end %>

          <%= link_to edit_my_review_path(@review), :class => 'button change_post', :title => 'Редактировать пост' do %>
            <span></span>
          <% end %>

          <%= link_to my_review_path(@review), :method => :delete, :class => 'button remove_post', :title => 'Удалить пост',
            :confirm => 'Вы уверены? Информация будет удалена без возможности восстановления!' do %>
            <span></span>
          <% end %>

        <% else %>
          <%= link_to 'В черновики', draft_my_review_path(@review), :method => :put, :class => 'button change_state',
            :confirm => 'Вы уверены? После подтверждения обзор будет недоступен пользователям сайта!' %>

          <%= link_to '#', :class => 'button change_poster', :title => 'Изменить постер' do %>
            <span></span>
          <% end %>

          <%= link_to edit_my_review_path(@review), :class => 'button change_post', :title => 'Редактировать пост' do %>
            <span></span>
          <% end %>
        <% end %>
      </div>
      <h3>Так обзор будет выглядеть при просмотре</h3>
    </div>

    <%= render :partial => 'reviews/review', :locals => { :review => @review } %>

    <ul class="tags">
      <% @review.tags.each do |item| %>
        <li><%= item %></li>
      <% end %>
    </ul>
  </div>

  <div class="right">
    <div class="header_with_marker">
      <h3>Так обзор будет выглядеть на списке</h3>
    </div>

    <ul class="posters">
      <li class="item"><%= render :partial => "reviews/poster", :locals => { :review => @review } %></li>
    </ul>

    <ul class="review_help">
      <%= render :partial => "help_#{@review.useful_type}" %>
    </ul>
  </div>

</div>
