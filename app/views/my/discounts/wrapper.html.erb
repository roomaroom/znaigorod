<div class="my_wrapper">
  <% content_for :yandex_map_scripts, javascript_include_tag('http://api-maps.yandex.ru/2.0.33/?load=package.full&lang=ru-RU') %>

  <%= render_navigation context: 'my', :renderer => :breadcrumbs %>

  <% if content_for?(:help) %>
    <h1>
      <%= t("my.#{resource.class.name.underscore}.title") %>: общая информация
      <%= link_to 'помощь', '#', :class => 'show_help' %>
    </h1>

    <div class='help_wrapper'>
      <div class='help'>
        <%= yield :help %>
      </div>
    </div>
  <% end %>

  <%= yield :my_content %>
</div>
