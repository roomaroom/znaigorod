<%= simple_form_for [:manage, @promotion] do |f| %>
  <%= f.input :url %>
  <%= f.button :submit, 'Сохранить', :class => 'button' %>
  или
  <%= link_to 'Отмена', @promotion.new_record? ? [:manage, :promotions] : [:manage, @promotion] %>
<% end %>
