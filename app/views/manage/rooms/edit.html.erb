<div class='form_view'>
  <h3>
    <%= link_to @organization, manage_organization_path(@organization) %> /
    <%= @room.title.present? ? @room.title : 'Без названия' %>
  </h3>

  <%= render 'form' %>
</div>
