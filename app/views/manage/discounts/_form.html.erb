<%= simple_form_for [:manage, @discount] do |f| %>
  <%= f.input :title %>
  <%= f.input :kind, :as => :check_boxes %>
  <%= f.input :description %>
  <%= f.input :discount %>
  <%= f.input :starts_at, :as => :date_picker %>
  <%= f.input :ends_at, :as => :date_picker %>

  <div class="actions">
    <%= link_to "&larr; Назад".html_safe, :back %>
    <%= f.submit 'Сохранить' %>
    <%= link_to 'Удалить'.html_safe, manage_discounts_path, method: :delete, confirm: 'Уверены что хотите удалить?', class: :destroy unless resource.new_record? %>
  </div>
<% end %>
