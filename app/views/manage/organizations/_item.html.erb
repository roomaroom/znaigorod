<li class="<%= cycle("even", "odd") %>">
  <%= link_to item.title, [:manage, item], :class => 'title' %>
  <span class="date"><%= l item.updated_at, :format => :short %></span>
  <span class="category"><%= item.category %></span>
</li>