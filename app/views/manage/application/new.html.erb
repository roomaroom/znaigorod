<%= simple_nested_form_for resource, :url => collection_path, :defaults => { :input_html => { :class => 'span8', :rows => 4 } }, :html => { :class => 'form-horizontal organization' } do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
<% end %>
