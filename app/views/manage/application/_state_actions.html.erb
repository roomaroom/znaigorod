<% resource.state_events.each do |state_event| %>
  <%= link_to t("activerecord.state_machines.state.events.#{state_event}"), polymorphic_path([:fire_state_event, :manage, resource], :event => state_event), :method => :put %>
<% end %>
