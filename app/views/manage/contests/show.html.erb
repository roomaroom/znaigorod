<div class="show">
  <h1>
    <%= resource.title %>
    <%= link_to t("edit_#{resource.class.model_name.underscore}"), [:edit, :manage, resource], :class => 'edit_link' %>
  </h1>

  <div class="column">
    <div>
      <span class="label">Описание:</span>
      <% if resource.description? %>
        <span class="value"><%= resource.description %></span>
      <% else %>
        <span class="blank">Не указано</span>
      <% end %>
    </div>

    <div>
      <span class="label">Дата начала:</span>
      <% if resource.starts_on? %>
        <span class="value"><%= resource.starts_on %></span>
      <% else %>
        <span class="blank">Не указано</span>
      <% end %>
    </div>

    <div>
      <span class="label">Дата окончания:</span>
      <% if resource.ends_on? %>
        <span class="value"><%= resource.ends_on %></span>
      <% else %>
        <span class="blank">Не указано</span>
      <% end %>
    </div>
  </div>
</div>

