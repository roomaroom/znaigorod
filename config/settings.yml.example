app:
  secret:               secret

solr:
  url:                  http://localhost:8982/solr

2gis:
  api_key:              api_key

vk:
  app_id:               app_id
  app_secret:           app_secret

google:
  app_id:               app_id
  app_secret:           app_secret

yandex:
  oauth_token:          token

  app_id:               app_id
  app_secret:           app_secret

facebook:
  app_id:               app_id
  app_secret:           app_secret

twitter:
  app_id:               app_id
  app_secret:           app_secret

odnoklassniki:
  app_id:               app_id
  app_secret:           app_secret
  app_public:           app_public

errors:
  key:                  api_key
  host:                 host

storage:
  url:                  http://storage.host

mail:
  address:              host
  port:                 25
  domain:               appname
  login:                user@host
  password:             secret
  from:                 noreply@host
  to:                   user@host

recaptcha:
  public:               token
  private:              token
