app:
  secret:               secret
  host:                 host.name
  port:                 8080
  url:                  http://host.name

solr:
  url:                  http://localhost:8982/solr

redis:
  url:                  redis://localhost:6379/12

2gis:
  api_key:              api_key

vk:
  app_id:               app_id
  app_secret:           app_secret

google:
  app_id:               app_id
  app_secret:           app_secret

yandex:
  oauth_token:          token

  app_id:               app_id
  app_secret:           app_secret

facebook:
  app_id:               app_id
  app_secret:           app_secret

twitter:
  app_id:               app_id
  app_secret:           app_secret

odnoklassniki:
  app_id:               app_id
  app_secret:           app_secret
  app_public:           app_public

mailru:
  app_id:               app_id
  app_private:          app_private

errors:
  key:                  api_key
  host:                 host
  url:                  http://errors.host

storage:
  url:                  http://storage.host

mail:
  address:              host
  port:                 25
  domain:               appname
  login:                user@host
  password:             secret
  from:                 noreply@host
  to:                   user@host
  to_afisha:            user@host

postmark:
  api_key:              key

recaptcha:
  public:               token
  private:              token

robokassa:
  login:                login
  secret_1:             secret1
  secret_2:             secret2

rbkmoney:
  account:              account

mainsms:
  project:              project
  api_key:              api_key

sms_claim:
  price:                1
  warning:              10
